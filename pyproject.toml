[tool.pyright]
include = ["backend", "ai", "tests"]
executionEnvironments = [
  { root = "." }
]
extraPaths = ["."]
venvPath = "."
venv = "venv"
pythonVersion = "3.11"
typeCheckingMode = "basic"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
